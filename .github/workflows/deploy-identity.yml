# Deploy Identity is now handled alongside Cloudflare Pages.
# This workflow file is DEPRECATED. Identity deploys via Docker/container in Pages Functions.
#
# For standalone Fly deployment (optional future enhancement):
# Create FLY_API_TOKEN at https://fly.io/user/personal_access_tokens
# Add as GitHub secret FLY_API_TOKEN if needed.
# See docs/08_Live_Identity_Setup.md for details.

name: Deploy Identity (Deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    name: Notice
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Identity deployment is now integrated with Cloudflare Pages. Fly deployment is optional."
