name = "ellines-eip"
compatibility_date = "2025-07-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "out"

# Do NOT add [triggers] crons here — Pages deploy fails on free/account cron limits
# and dual-trigger races. Connector due-sync runs when Org IT opens Connectors
# (POST /api/v1/connectors/run-due). Keep this file deploy-safe.
